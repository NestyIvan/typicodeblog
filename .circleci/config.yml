version: 2.1
jobs:
  build:
    docker:
      - image: circleci/openjdk:11.0.3-jdk-stretch
    steps:
      - checkout
      - run: ./gradlew test
      - store_test_results:
        path: build/test-results/test
      - store_artifacts:
        path: build/test-results/test
